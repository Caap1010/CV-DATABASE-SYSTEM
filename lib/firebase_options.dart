// Copyed placeholder generated from firebase_options_template.dart
// Replace these values by running `flutterfire configure` or filling
// them with your Firebase project's actual configuration.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  // Placeholder Firebase config for web. Replace the values below.
  static const FirebaseOptions web = FirebaseOptions(
    apiKey: 'REPLACE_WITH_API_KEY',
    authDomain: 'REPLACE_WITH_AUTH_DOMAIN',
    projectId: 'REPLACE_WITH_PROJECT_ID',
    storageBucket: 'REPLACE_WITH_STORAGE_BUCKET',
    messagingSenderId: 'REPLACE_WITH_MESSAGING_SENDER_ID',
    appId: 'REPLACE_WITH_APP_ID',
  );
}

// Note: Run `flutterfire configure` to generate a full
// `lib/firebase_options.dart` including platform-specific options.
